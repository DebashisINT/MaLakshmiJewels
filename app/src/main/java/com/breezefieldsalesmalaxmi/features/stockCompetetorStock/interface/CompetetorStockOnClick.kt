package com.breezefieldsalesmalaxmi.features.stockCompetetorStock.`interface`

interface CompetetorStockOnClick {
    fun stockListOnClickView(stockID:String)
    fun stockListOnClickSync(stockID:String)
}